# Core runtime
requests>=2.28,<3.0
rich>=13.0,<16.0  # <14 clashed with textual>=8 (needs rich>=14.2)
huggingface_hub>=0.20,<1.0
psutil>=5.9,<6.0
pydantic>=2.0,<3.0
langgraph>=1.0,<2.0
langgraph-checkpoint>=2.0,<3.0
textual>=8.0,<9.0  # Module Manager TUI (manager_tui.py)

# Lab / Blue Team runtime
flask>=2.3,<4.0
flask_cors>=4.0,<5.0
PyJWT>=2.8,<3.0

# Module packs (vendored: suijin/modules/google_dork)
duckduckgo-search>=6.0

# Optional: Neo4j knowledge-graph backend (config kg_backend=neo4j)
# pip install neo4j  # uncomment with the switch when a server exists

# Desktop app gateway (localhost; Tauri client connects here)
fastapi>=0.115
uvicorn>=0.30
websockets>=13

# Optional providers (uncomment to enable)
# google-genai>=1.0  # Gemini
# anthropic>=0.30    # Anthropic

# Testing & dev deps (pytest, pytest-asyncio, pytest-cov) are installed
# separately — see .github/workflows/ci.yml. They do not ship with the app.
